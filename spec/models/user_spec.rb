 require 'rails_helper'
 RSpec.describe User, type: "model" do
  # it "Allows creation of individual user and account " do
  #     user = User.create(email: "pr@gmail" , password: "123456")
  #     account = Account.create(name: "Flipkart" , user_id: user.id)
  #   end
    # it "allows updation in invitation table " do
    #   invitat = Invitation.create(acc_id: 1)
    #     user = User.create(email: "pr@gmail" , password: "123456")
    #     #account = Account.create(name: "Flipkart" , user_id: user.id)
    #     invitation = Invitation.where(id: invitat.id).where(invite_email: user.email).last.update(mem_id: user1.id)
    #     inv = User.account_create(invitation)
    #   end
end
